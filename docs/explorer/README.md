# S4I AoW2 — Scaling Track Explorer (team-facing)

`index.html` is the self-contained interactive explorer (no build step, no dependencies).

Once GitHub Pages is enabled (main branch, /docs folder), this publishes at:
**https://egirvetz.github.io/scaling-intelligence-platform/explorer/**

Only the TEAM-facing version lives here (no funding, no engagement notes). Keep the confidential
version out of any public repo.
